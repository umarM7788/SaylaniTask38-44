function customPower(a, b) {
    return Math.pow(a, b);
  }
  
  var a = 2;
  var b = 3;
  var result = customPower(a, b);
  document.write(a + " raised to the power of " + b + " is " + result);
  